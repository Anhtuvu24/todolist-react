import React from 'react';

function paginationSub(WrappedComponent, todoList) {
    return class extends React.Component {
        getNewer = () => {
            debugger;
        }
    }
}

export default paginationSub;